<!-- @import "[TOC]" {cmd="toc" depthFrom=2 depthTo=3 orderedList=false} -->

<!-- code_chunk_output -->

- [markdown 入门](#markdown-入门)
- [定位组](#定位组)
- [机构组](#机构组)
  - [学习步骤](#学习步骤)
  - [注意事项](#注意事项)
  - [PID 学习](#pid-学习)
  - [电机三环控制](#电机三环控制)
  - [3508 电机——直流无刷减速电机](#3508-电机直流无刷减速电机)
  - [编码器](#编码器)
  - [模式](#模式)
- [主控](#主控)
  - [学习计划](#学习计划)
  - [任务学习](#任务学习)
  - [底盘学习](#底盘学习)
  - [每日记录](#每日记录)


# 机器人队学习之旅

## markdown 入门

1. 代码输入

   ```c
   return 0;
   ```

   或者正文中的代码 `hello`
2. 列表
   有序输入："x."+空格
   无序输入："-"+空格
3. 常见整理
   [规则参考](https://github.com/mdickin/vscode-markdown-shortcuts/blob/master/README.md)
   [表格工具](https://www.tablesgenerator.com/markdown_tables)

## 定位组

#### AD转化

##### ADS8320

基本图片
![表格](/img/2021-02-20-09-02-28.png)

封装
![无](/img/2021-02-20-09-02-28.png)
![](/img/2021-02-20-09-02-26.png)

###### SPI

是不是F4芯片上CS只是在F4作为从设备时才用上的

SPI配置:我们SPI通信有4种不同的模式，不同的从设备可能在出厂是就是配置为某种模式，这是不能改变的；但我们的通信双方必须是工作在同一模式下，所以我们可以对我们的主设备的SPI模式进行配置，通过CPOL（时钟极性）和CPHA（时钟相位）来控制我们主设备的通信模式

CPOL=0，表示当SCLK=0时处于空闲态，所以有效状态就是SCLK处于高电平时
CPOL=1，表示当SCLK=1时处于空闲态，所以有效状态就是SCLK处于低电平时
CPHA=0，表示数据采样是在第1个边沿，数据发送在第2个边沿
CPHA=1，表示数据采样是在第2个边沿，数据发送在第1个边沿

##### AD7705

![](img/2021-02-22-08-02-25.png)
![](img/2021-02-22-08-02-27.png)

1. 16位无丢失代码性AD转化
2. 芯片5V供电时,REF建议电压范围是1V~3.5V，推荐值2.5V；
3. 芯片3V供电时,REF建议电压范围是1V~1.75V，推荐值1.225V，模块上参考电压芯片是REF192-2.048，也就是2.048V，显然都不是推荐值，且这已经超出了3V供电的推荐范围，因此电路设计不规范。
4. 双通道差分输入
5. 基准电压为2.5V时，在单极性信号下，输入范围是0到2.5V，在双极性输入下，输入范围是-1.25到+1.25
6. 低功耗CMOS芯片，功耗一般为20uW
7. 可编程，可编程增益，以及降噪参数等。

NOTICE:

1. 硬件复位

```C
 static void TM7705_ResetHard(void)	//硬件复位
{
	TM7705_RST = 1;
	delay_ms(1);
	TM7705_RST = 0;
	delay_ms(2);
	TM7705_RST = 1;
	delay_ms(1);
}
```

2. 时序校准![](img/2021-02-27-21-02-65.png)

```C
static void TM7705_SyncSPI(void)      //同步SPI接口时序
{
	TM7705_CS = 0;
	SPI2_ReadWriteByte(0xFF);//32个1
	SPI2_ReadWriteByte(0xFF);
	SPI2_ReadWriteByte(0xFF);
	SPI2_ReadWriteByte(0xFF);

	TM7705_CS = 1;
}
```

3. 通过SPI发送命令进行芯片**寄存器配置**
   寄存器
   第一个是通信寄存器，它管理通道选择，决定下一个操作是读操作还是写操作，以及下一次读或写哪一个寄存器。所有与器件的通信必须从写入通信寄存器开始。上电或复位后，器件等待在通信寄存器上进行一次写操作。这一写到通信寄存器的数据决定下一次操作是读还是写，同时决定这次读操作或写操作在哪个寄存器上发生。所以，写任何其它寄存器首先要写通信寄存器，然后才能写选定的寄存器。所有的寄存器（包括通信寄存器本身和输出数据寄存器）进行读操作之前，必须先写通信寄存器，然后才能读选定的寄存器。此外，通信寄存器还控制等待模式和通道选择，此外DRDY状态也可以从通信寄存器上读出。第2个寄存器是设置寄存器，决定校准模式、增益设置、单/双极性输入以及缓冲模式。第3个寄存器是时钟寄存器，包括滤波器选择位和时钟控制位。第4个寄存器是数据寄存器，器件输出的数据从这个寄存器读出。

## 机构组

### 学习步骤

![](img/2021-02-19-21-02-77.png)

### 注意事项

1. 输入信号模式：50-500HzPWM 信号或 CAN 总线通信**关闭**产品电源以设置信号模式，然后**重新启动**以应用新模式。
2. 快速设置，为连接到总线的所有速度控制器分配 ID 号；通过 CAN 总线通讯反馈电动机温度，转子位置和转速；更换电动机时无需位置传感器校准**仅在使用 CAN 总线通讯时才能获取有关转子位置和转速的信息**
3. **请勿**同时插入 CAN 电缆和 PWM 电缆
4. 先插 7pin，再三相线，再 CAN/PWM，最后通电

### PID 学习


| 缩写  |     英文     | 备注  |
| :---: | :----------: | :---: |
|   P   | proportional | 比例  |
|   I   |   integral   | 积分  |
|   D   |  derivative  | 微分  |

#### 位置式 PID 控制算法

**适用于执行机构不带积分部件的对象，如舵机和平衡小车的直立和温控系统的控制**
因为有误差积分 ∑e(i)，一直累加，也就是当前的输出 u(k)与过去的所有状态都有关系，用到了误差的累加值；（误差 e 会有误差累加），输出的 u(k)对应的是执行机构的实际位置，，一旦控制输出出错(控制对象的当前的状态值出现问题 )，u(k)的大幅变化会引起系统的大幅变化

并且位置式 PID 在积分项达到饱和时,误差仍然会在积分作用下继续累积，一旦误差开始反向变化，系统需要一定时间从饱和区退出，所以在 u(k)达到最大和最小时，要停止积分作用，并且要有积分限幅和输出限幅
所以在使用位置式 PID 时，一般我们直接使用 PD 控制

![](img/2021-02-20-20-02-48.png)
_P× 当前误差 + I× 误差累加 + D× 误差增量_

注意**积分限幅**

```C
typedef struct PID
{
  float P,I,D,limit;
}PID;

typedef struct Error
{
  float Current_Error;//当前误差
  float Last_Error;//上一次误差
  float Previous_Error;//上上次误差
}Error;

/*!
 *  @brief      位置式PID
 *  @since      v1.0
 *  *sptr ：误差参数
 *  *pid:  PID参数
 *  NowPlace：当前位置
 *  Point：   预期位置
 */

// 位置式PID控制
float PID_Realize(Error *sptr,PID *pid, int32 NowPlace, float Point)
{

    int32 iError,	// 当前误差
         Realize;   //实际输出

    iError = Point - NowPlace;	// 计算当前误差
    sptr->Current_Error += pid->I * iError;	// 误差积分
      sptr->Current_Error = sptr->Current_Error > pid->limit?pid->limit:sptr->Current_Error;//积分限幅
      sptr->Current_Error = sptr->Current_Error <-pid->limit?-pid->limit:sptr->Current_Error;
    Realize = pid->P * iError       //比例P
            + sptr->Current_Error   //积分I
            + pid->D * (iError - sptr->Last_Error);  //微分D
    sptr->Last_Error = iError;		  	// 更新上次误差
    return Realize;	// 返回实际值
}
```

markdown 段落缩进

#### 增量式 PID 控制算法

一旦确定了 KP、TI 、TD，只要使用前后三次测量值的偏差， 即可由公式求出控制增量

![](img/2021-02-20-23-02-09.png)
而得出的控制量 ▲u（k）对应的是近几次位置误差的增量，而不是对应与实际位置的偏差没有误差累加也就是说，增量式 PID 中不需要累加。控制增量 Δu（k）的确定仅与最近 3 次的采样值有关，容易通过加权处理获得比较好的控制效果，并且在系统发生问题时，增量式不会严重影响系统的工作总结：增量型 PID，是对位置型 PID 取增量，这时控制器输出的是相邻两次采样时刻所计算的位置值之差，得到的结果是增量，即在上一次的控制量的基础上需要增加（负值意味减少）控制量。

### 电机三环控制

![三环控制](image/机构组学习/1614906390595.png)

![流程图](image/机构组学习/1614906420394.png)

伺服驱动器的设计可尽量确保电流环具备良好的反应性能，故用户只需调整**位置环与速度环的增益**即可。
通常来说，要求位置环的反应不能快于速度环的反应。因此，若要增加位置环的增益，必须先增加速度环的增益。如果只增加位置环的增益，电机很可能产生震动，从而将会造成速度指令及定位时间的增加，而非期望的减少

如果位置环反应比速度环反应还快，由于速度环反应相对较慢，速度环的输出变化无法跟上位置环输出的速度指令的变化，因此就无法达到平滑的线性加速或减速。而且，位置环会继续累计脉冲偏差，从而增加速度指令。这样，电机速度会超过给定值，然后位置环会尝试减少速度指令输出量，这样又会导致速度环反应会变得很差，电机将赶不上速度指令。整个速度指令会如图1所示振动。如果发生这种情形，就必须减少位置环增益，或增加速度环增益，以防止速度指令振动。

![示意图](image/机构组学习/1614906838508.png)

### 3508 电机——直流无刷减速电机

#### 外观

![](img/2021-02-18-23-02-87.png)

1. 7pin端口,用于交换数据
2. 三相电源线缆,连接3508电机
3. 120Ω电阻开关
4. **配置速度控制器**
5. 电源线
6. 指示灯
7. CAN连接口，注意速度也是**1Mbp**
8. PWM端口

#### 基本操作

1. 指示灯
   D:\机器人队\8.机构组资料\电机\M3508
   《RM C620 无刷电机调速器使用说明（中英日）V1.01》P10
2. 使用设置旋钮 p12

   - ID 单独设置：（**1-8**）**同一总线上 ID 号不能重复**


   | 序号  |        操作        | 指示灯 |
   | :---: | :----------------: | :----: |
   |   1   |   按一次激活模式   | 灯全灭 |
   |   2   |  按的次数是 ID 号  |  橙光  |
   |       | 三秒无操作自动保存 |        |
   |   3   |        重启        |        |


   - 快速批量设置


   | 序号  |                           |            |
   | :---: | :-----------------------: | :--------: |
   |   1   |          按一次           |   灯全灭   |
   |   2   |           长按            | 所有灯变绿 |
   |   3   | 按 ID 号依次拨动至少 180° |            |
   |   4   |           重启            |            |
3. 较准
   **更换**M3508 或速度控制器后校准 M3508
   按下 set 键直至绿灯闪烁，在校准期间，电机将继续旋转建议校准无有效载荷的电机
4. CAN 通讯规则
   **CAN数据接收：**
   ![](image/机构组学习/1614931024191.png)
   **发送频率**1kHz，1ms一次，我们定时器中断发送电流命令基本上也是1ms一次（实际上小于1ms）
   **机械角度：** 0-8191，这儿我们认为是编码器线数8192
   **转速：** RPM，r转每分钟

   ```cpp
   M3508instrin.PULSE = 8192;
   M3508instrin.RATIO = 19;
   M3508instrin.CURRENT_LIMIT = 14745;
   ```

   CAN发送命令：两条不同 `StdId`的CAN报文一共最多控制8个电机，即0x200 对应前 4 个 ID，0x1FF 对应后 4 个 ID，控制电流值范围-16384~16384，对应电调输出的转矩电流范围-20A–20A。电流环认为是在驱动器中自己实现了，我们无须管，控制了电流其实就控制了转矩

   ![](image/机构组学习/1614930615643.png)

   ![](image/机构组学习/1614930645523.png)

   ```c
   //y一个CAN可以控制8个电机,每个电机有固定的ID号1-8
       if ((rx_message.StdId >= 0x201) && (rx_message.StdId <= 0x208) && (rx_message.RTR == CAN_RTR_Data)) //DJ电机报文处理
       {
           u8 id = rx_message.StdId - 0x201; //y motor.h中定义0x201-0x208
                                             //y 说明书,先发高8位再发低8位
           ChangeData(&rx_message.Data[0], &rx_message.Data[1]);
           ChangeData(&rx_message.Data[2], &rx_message.Data[3]);
           ChangeData(&rx_message.Data[4], &rx_message.Data[5]);
           //y 认为电机反馈的就是真实值
           //y 将16位数据通过取地址方式发送
           DecodeS16Data(&motor[id].valueReal.pulseRead, &rx_message.Data[0]); //y 转子机械角度
           DecodeS16Data(&motor[id].valueReal.speed, &rx_message.Data[2]);		//y 转子转速
           DecodeS16Data(&motor[id].valueReal.current, &rx_message.Data[4]);	//y 实际转矩电流
                                                                               //y data[6] 电机温度
                                                                   //y data[7] NULL
           //y 电机机械角度与实际位置还差了个编码器线数PULSE和减速比RATIO
           //y motor[id].valueReal.pulse+=motor[id].argum.distance;//累计脉冲计算
           //y 电调返回的是脉冲值
           motor[id].valueReal.angle = motor[id].valueReal.pulse * 360.f / motor[id].intrinsic.RATIO / motor[id].intrinsic.PULSE;
           motor[id].argum.lastRxTim = OSTimeGet();
       }
   ```

#### 小知识

1. DT 的程序中很多用的是#define DT_SELECT 0
   后面判断#if
   机构的程序是先 `//#define USE_ELMO`

   ```
   #if defined USE_ELMO | defined USE_VESC | defined USE_EPOS
   CAN_ITConfig(CAN2, CAN_IT_FMP1, ENABLE); //ELMO的CAN邮箱使能
   #endif
   ```

### 编码器

**旋转编码器**也称为**轴编码器**，是将旋转位置或旋转量转换成模拟或数字信号的[机电](https://www.wiki-wiki.top/baike-%E6%A9%9F%E9%9B%BB "机电")设备。

增量型编码器：相对型编码器（relative encoder），利用检测脉冲的方式来计算转速及位置，可输出有关旋转轴运动的信息——无法在转轴不动时得到转轴旋转位置的信息

绝对型编码器：将转轴的不同位置加以编号，再依目前转轴位置输出对应的编号——随时可以知道转轴的位置

### 模式

1. 速度模式
2. 位置模式
3. 寻零模式
   电机缓慢转动，碰到机构或啥的就停止了。然后将脉冲计数置0，转入位置模式
4. 无

## 主控

### 学习计划

![](image/机构组学习/1615115142787.png)

### 任务学习

具有私有空间的任务就叫做进程，没有私有空间的任务就叫做线程。从任务的组成上来看，μCOS-II没有给任务定义私有空间，因此μC/OS-II中的所有任务都属于线程。![](image/机构组学习/1615115032273.png)

最多可以含有64 个任务（包括用户任务和系统任务）

任务状态：![任务的状态](image/机构组学习/1615115340327.png)

任何时刻只能有一个任务处于运行状态，就绪的任务只有当所有优先级高于本任务的任务都转为等待状态时，才能进入运行状态

```cpp
static void TaskStart(void *pdata)
{
	OS_CPU_SR cpu_sr = 0;
	pdata = pdata;
	OS_CPU_SysTickInit();
	OS_ENTER_CRITICAL(); //进入临界区（无法被中断打断）
	RUN = OSSemCreate(0);
	Beep_Show(2); //上电提醒
	Led_Show();
	OSTaskCreate(TaskCurveFit, (void *)0, &CurveFit_task_stk[CurveFit_STK_SIZE - 1], CurveFit_TASK_PRIO);
	OSTaskCreate(Task_Lcd, (void *)0, (OS_STK *)&LCD_TASK_STK[LCD_STK_SIZE - 1],LCD_TASK_PRIO);
	OSTaskSuspend(START_TASK_PRIO); //挂起起始任务
	OS_EXIT_CRITICAL();				//退出临界区
}

// LCD任务
static void Task_Lcd(void *pdata) 
{
	pdata=pdata;

	while (1) 
	{
		Led_Show();
		OSTimeDly(1000);
	}
}
```

![基本模板](image/机构组学习/1615117563615.png)

可以在μC/OS-II中创建最多**64个**任务，所以**任务的优先级别最多有64级**，每个级别都用一个整数数字来表示，即0、1、2、,,、63。数字越小，优先级别越高。于大多数应用程序的任务数小于64，所以为了使用户可以设置所需要任务的实际数目，μC/OS-IⅡI在系统配置文件OS_CFG.H中定义了一个用来表示最低优先级别的常数OS_LOWEST_PRIO，如果用户为其赋了值，那么就意味着系统中可供使用的优先级别为0、1、2…、OS_LOWEST_PRIO，对应地，任务的总数也不能超过OSLOWESTPRIO十1个。另外，为了用户的方便，系统总是把最低优先级别OS_LOWEST_PRIO自动赋给空闲任务。如果应用程序使用了统计任务，则系统还会把优先级别OS_LOWEST_PRIO-1自动赋给统计臼务，因此用户任务可以使用的优先级别是0、1、2、…、OS_LOWEST_PRIO-2，共OS_LOWESTRIO-1个。当然，用户任务的优先级别要由用户在创建一个任务时显式地定义。

当调用函数OSTaskCreate（）来创建一个任务时，把数组的指针传递给函数OSTaskCreate（）中的堆栈栈顶参数ptos，就可以把该数组与任务关联起来而成为该任务的任务堆栈。

### 底盘学习
#### 双驱带转向直轮底盘

一个舵机带动平行四杆机构使两从动前轮同步转向
双驱————加减速性能较为一般，由于转向舵机的存在能够实现比较灵活的转弯，但是需要较大的转弯半径，不具备全向移动的能力，难以在短时间内通过遥控或者自动的方式调整到指定位置，因此较少出现在需要全场定位或者需要到达精准位置进行作业任务的场合。比较常用于通过巡线的方式走固定轨迹的场合

#### 四驱无转向直轮底盘

驱动部分采用四个电机驱动，每一个轮子单独驱动不存在专门用于转向的自由度

该底盘由于没有专门用于转向的自由度，因此在转向控制上较为困难，多采用四轮差速在接触面上**摩擦**来转向，但与地面的摩擦难以控制，因此转向较为僵硬，很难实现在短时间内精准转向并且到达目标位置，不利于对环境中具体目标的作业。该底盘由于采用四轮驱动因此加减速能力较强，但很难在行进的过程中调整航向角，因此比较难应用于高速运动场合，

#### 四轮同步转向底盘
个转向电机通过传动机构带动四个驱动轮同时转过相同的角度以实现转向。该底盘的四个驱动轮在转向时只能转动同样的自由度，因此在转向的时候不符合阿克曼转向原理，需要通过差速的方式使轮子出现一定的打滑才能完成转向，除此之外该底盘不能很好的实现原地的旋转运动，因此该底盘在灵活性上存在一些不足。该底盘由于是四轮驱动且不存在自身轮系驱动力的分力抵消，因此在直线运动时加减速能力很强，具备很大的优势，但对于需要灵活转向与旋转的场合上则显得有些吃力

##### 阿克曼基本原理
汽车在行驶过程中（直线和转弯时候），每个车轮的运动估计必须符合他的自然运动轨迹，从而保证轮胎与地面始终处于纯滚动。

### 程序学习

1. CAN之间是可以互相通信的，A板子CAN2/CAN1对B板子无意义。eg：CAN1可以用来与传感器（DT/码盘），机构等通信；CAN2可以用来给底盘（3驱动+3转向）+4翻越通信
2. 不同串口也都用到了
```C
	USART1_Configuration(); //用于虚拟示波器
	USART2_Configuration(); //用于串口显示屏手柄
	USART3_Configuration(); //串口屏备份
	USART4_Configuration(); //用于摄像头
	USART5_Configuration(); //用于双车通信
```




### 每日记录

#### 2021.3.14

1. 重新看底盘代码，TR是三驱动电机+三转向电机；DR是3驱动电机+3转向电机+4条腿。对于TR来说，CAN1用来和主控通信，CAN2用来直接控制各个电机。
2. EC电机，即永磁无刷直流（BLDC）电动机。maxon系列的，有EC-4pole-30，ECflat
3. EC-4pole-30，30是指电机直径30mm

#### 2021.3.13
CAN总线有接收邮箱和发送邮箱：
”发送邮箱“是用于CAN总线数据发送的，总共有3个，并且存在优先级关系。优先级越高表示其里面的数据会被优先发送。数据在发送前都会被送到优先级最高且空闲的发送邮箱，然后依次发送。最后说明一点：“**发送邮箱有3个，且每个邮箱只能装一个报文**”。
”接收邮箱“是用于CAN总线数据接收用的，在接收数据端会有一个过滤器处于”接收邮箱“的前面，过滤器使用于删选”标识符“的，只有标识符符合的报文才会被放入到”接收邮箱“当中。注意：”接收邮箱不同于发送邮箱，接收邮箱只有2（FIFO0、FIFO1）个，但是每一个有三层，每层都可以存放一个报文，即每一个接收邮箱可以接收三个报文。但读取时只能读到最先收到的报文，等这个读完之后，才能读下一个报文”。

#### 2021.3.12
1. 学习队列，原来有两种，queue与cancontrollist不同，后者要收到反馈才行，慢慢学习代码中
2. 发现`if - else`比`if if`好用些，最少可以只判断一次，而后面的就得次次判断。
3. queue与cancontrollist
   1. 每一条can队列都有队头,队尾,N个CAN小成员;
   2. 每一条cancontrollist队列除了同queue一样的头,尾,内容外(这一部分**直接引用Can_QueueTypeDef结构体**)，还多了的是对can控制块内结构体的调用
   3. 在
4. 首先，定义一个存储了一条CAN总线上所有节点信息的数组`MesgControlGrpTypeDef Can1_MesgSentList[CAN1_NodeNumber];`
针对每一个节点Node，或者说每一个发送/接收CAN报文对象，都有发送标志位（成功/失败/待发送数），也都有等待发送的多条CAN报文——这些等待发送的CAN报文也组成节点内的小队列queue（有front，有rear，有n条报文）
```C
//报文控制块
typedef struct
{
	s32 SendNumber;
	s32 ReceiveNumber;
	u32 QUEUEFullTimeout;
	u8 TimeOut;
	s32 SendSem;
	Can_QueueTypeDef SentQueue; //CAN报文队列结构体再命名为SentQueue
} MesgControlGrpTypeDef;
```
```
//CAN报文队列
typedef struct
{
	uint16_t Front, Rear;
	Can_SendstructTypeDef Can_DataSend[CAN_QUEUESIZE]; 
} Can_QueueTypeDef;	
```
#### 2021.3.11

1. PID不能停，然后run point等函数仅仅进行状态标志位控制
2. 学习队列相关操作
#### 2021.3.10
1. 知道为什么DT用`if DT_SELECT == 1`进行判断了,DT的各种状态是互斥的，而对于机构组，多个任务可以同时进行，用`ifdef`配合macro.h进行宏定义就很棒
2. 试图用github desktop版本回退失败
#### 2021.3.9
学到一点了
```C
typedef struct
{
    vs16 angle;     //轴前角度
    vs16 current;   //电流
    vs16 speed;     //速度 (rpm)
    vs32 pulse;     //累计脉冲
    vs16 pulseRead; //反馈脉冲
    vs8 tempeture;  //温度
} MotorValue;
```
查找vs16定义
`typedef __IO int16_t  vs16;`
再看__IO定义
`#define     __IO    volatile   `
volatile的作用是作为指令关键字，确保本条指令不会因编译器的优化而省略，那么用vs应该就是为了告诉编译器不要优化我，，，
#### 2021.3.5

![电机的三环控制](img/2021-03-05-08-03-70.png)

![三环](img/2021-03-05-08-03-12.png)

#### 2021.3.1--memcpy与队列用法

```C
/****接收反馈原报文****/
static void answer_master(CanRxMsg *rx_message)
{
	CanTxMsg tx_message;

	tx_message.IDE = CAN_Id_Standard;
	tx_message.StdId = rx_message->StdId - 0x80;
	tx_message.RTR = CAN_RTR_Data;
	tx_message.DLC = rx_message->DLC;
	memcpy(&tx_message.Data[0], &rx_message->Data[0], rx_message->DLC);//y 这个想法不错啊

	CAN_Transmit(CAN1, &tx_message);
}
```

大量CAN报文一起发送可能会出问题,现在是将数据存放在报文列表中，定时器中定时进行发送

```C
void SteerMotor_Answer_PX(u8 ID, float angle, u8 InConGrpFlag)
{
	TempData.s32_temp1 = (s32)(angle * 100);
	if (Rear1 == Can1_Sendqueue.Front)
	{
		flag.Can1SendqueueFULL++;
		return;
	}
	else
	{
		Can1_Sendqueue.Can_DataSend[Can1_Sendqueue.Rear].ID = 0x280 + ID;
		Can1_Sendqueue.Can_DataSend[Can1_Sendqueue.Rear].DLC = 0X08;
		Can1_Sendqueue.Can_DataSend[Can1_Sendqueue.Rear].Data[0] = 'P';
		Can1_Sendqueue.Can_DataSend[Can1_Sendqueue.Rear].Data[1] = 'X';
		Can1_Sendqueue.Can_DataSend[Can1_Sendqueue.Rear].Data[2] = 0;
		Can1_Sendqueue.Can_DataSend[Can1_Sendqueue.Rear].Data[3] = 0;
		EncodeS32Data(&TempData.s32_temp1, &Can1_Sendqueue.Can_DataSend[Can1_Sendqueue.Rear].Data[4]);

		Can1_Sendqueue.Can_DataSend[Can1_Sendqueue.Rear].InConGrpFlag = InConGrpFlag;
	}
	Can1_Sendqueue.Rear = Rear1;
}
```

#### 2020.2.28

![注意信号方向](img/2021-02-28-08-02-44.png)
之前以为可以直接将CS接地而不进行控制，后面发现必须要用CS作为等待数据转化的标记，但此时第一版板子已投，后面设想用DRDY进行辅助判断，但后面发现DRDY是输出端，也没办法用

![](img/2021-02-27-22-02-06.png)

因为有寄存器需要配置，我们有写寄存器与读寄存器操作，那么MISO与MOSI都是要配置啊。之前AD8320只有输出就没有连接MOSI(连程序都没配，，，)

#### 2020.2.26

[复用](https://blog.csdn.net/weixin_43554366/article/details/97704865)

![](img/2021-02-26-08-02-16.png)

宏定义USE_STDPERIPH_DRIVER，就能包含头文件stm32f0xx_conf.h了

[详见博客](https://www.cnblogs.com/leo0621/p/9435794.html)

关于GPIO_Init与GPIO_PinAFConfig函数的问题

```C
  GPIO_InitStructure.GPIO_Pin = GPIO_Pin_3 | GPIO_Pin_4; //PB13,PB14复用功能输出
  GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF;             //复用功能
  GPIO_InitStructure.GPIO_OType = GPIO_OType_PP;           //推挽输出
  GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;        //50MHz
  GPIO_InitStructure.GPIO_PuPd = GPIO_PuPd_UP;             //上拉
  GPIO_Init(GPIOB, &GPIO_InitStructure);                   //初始化   
```

```C
  GPIO_PinAFConfig(GPIOB, GPIO_PinSource3, GPIO_AF_SPI1); //PB3复用为 SPI1
  GPIO_PinAFConfig(GPIOB, GPIO_PinSource4, GPIO_AF_SPI1); //PB4复用为 SPI1
```

**注意**：不能想当然地认为可以写成
`GPIO_PinAFConfig(GPIOB, GPIO_PinSource3|GPIO_PinSource4, GPIO_AF_SPI1);`
当时分开写无问题，合起来后直接数据无反应，debug进入函数内部查询

```
void GPIO_PinAFConfig(GPIO_TypeDef* GPIOx, uint16_t GPIO_PinSource, uint8_t GPIO_AF)
{
  uint32_t temp = 0x00;
  uint32_t temp_2 = 0x00;
  
  /* Check the parameters */
  assert_param(IS_GPIO_ALL_PERIPH(GPIOx));
  assert_param(IS_GPIO_PIN_SOURCE(GPIO_PinSource));
  assert_param(IS_GPIO_AF(GPIO_AF));
  
  temp = ((uint32_t)(GPIO_AF) << ((uint32_t)((uint32_t)GPIO_PinSource & (uint32_t)0x07) * 4)) ;
  GPIOx->AFR[GPIO_PinSource >> 0x03] &= ~((uint32_t)0xF << ((uint32_t)((uint32_t)GPIO_PinSource & (uint32_t)0x07) * 4)) ;
  temp_2 = GPIOx->AFR[GPIO_PinSource >> 0x03] | temp;
  GPIOx->AFR[GPIO_PinSource >> 0x03] = temp_2;
}
```

发现打断点看到 `GPIOx->AFR[GPIO_PinSource >> 0x03] &= ~((uint32_t)0xF << ((uint32_t)((uint32_t)GPIO_PinSource & (uint32_t)0x07) * 4)) ;`前GPIO_PinSource==7，进入stm32f4xx_gpio.h，发现有如下定义：

```C
#define GPIO_PinSource3            ((uint8_t)0x03)
#define GPIO_PinSource4            ((uint8_t)0x04)
```

此时意识到该函数可能将 `GPIO_PinSource4|GPIO_PinSource4`认为是 `0x03|0x04`，即 `0000,0011B|0000,0100B = 0000,0111B = 7D`,此时基本水落石出啦

但为什么GPIO_Init函数没这个问题呢？
进入函数内部：

```
void GPIO_Init(GPIO_TypeDef* GPIOx, GPIO_InitTypeDef* GPIO_InitStruct)
{
  uint32_t pinpos = 0x00, pos = 0x00 , currentpin = 0x00;

  /* Check the parameters */
  assert_param(IS_GPIO_ALL_PERIPH(GPIOx));
  assert_param(IS_GPIO_PIN(GPIO_InitStruct->GPIO_Pin));
  assert_param(IS_GPIO_MODE(GPIO_InitStruct->GPIO_Mode));
  assert_param(IS_GPIO_PUPD(GPIO_InitStruct->GPIO_PuPd));

  /* -------------------------Configure the port pins---------------- */
  /*-- GPIO Mode Configuration --*/
  for (pinpos = 0x00; pinpos < 0x10; pinpos++)
  {
    pos = ((uint32_t)0x01) << pinpos;
    /* Get the port pins position */
    currentpin = (GPIO_InitStruct->GPIO_Pin) & pos;

    if (currentpin == pos)
    {
      GPIOx->MODER  &= ~(GPIO_MODER_MODER0 << (pinpos * 2));
      GPIOx->MODER |= (((uint32_t)GPIO_InitStruct->GPIO_Mode) << (pinpos * 2));

      if ((GPIO_InitStruct->GPIO_Mode == GPIO_Mode_OUT) || (GPIO_InitStruct->GPIO_Mode == GPIO_Mode_AF))
      {
        /* Check Speed mode parameters */
        assert_param(IS_GPIO_SPEED(GPIO_InitStruct->GPIO_Speed));

        /* Speed mode configuration */
        GPIOx->OSPEEDR &= ~(GPIO_OSPEEDER_OSPEEDR0 << (pinpos * 2));
        GPIOx->OSPEEDR |= ((uint32_t)(GPIO_InitStruct->GPIO_Speed) << (pinpos * 2));

        /* Check Output mode parameters */
        assert_param(IS_GPIO_OTYPE(GPIO_InitStruct->GPIO_OType));

        /* Output mode configuration*/
        GPIOx->OTYPER  &= ~((GPIO_OTYPER_OT_0) << ((uint16_t)pinpos)) ;
        GPIOx->OTYPER |= (uint16_t)(((uint16_t)GPIO_InitStruct->GPIO_OType) << ((uint16_t)pinpos));
      }

      /* Pull-up Pull down resistor configuration*/
      GPIOx->PUPDR &= ~(GPIO_PUPDR_PUPDR0 << ((uint16_t)pinpos * 2));
      GPIOx->PUPDR |= (((uint32_t)GPIO_InitStruct->GPIO_PuPd) << (pinpos * 2));
    }
  }
}
```

感觉这个比 `GPIO_PinAFConfig`要复杂挺多的，后面看看博客，或是请教下魏航星大佬
//TODO:GPIO_Init

当CS/SHDN拉低``
![](img/2021-02-26-09-02-35.png)

```
int Read_ADC1_Value(void)
{
  int adc_value1 = 0;
  int ad_h = 0, ad_l = 0;

  GPIO_ResetBits(GPIOA, GPIO_Pin_15); //片选信号，低电平触发，ADS8320进行一次数据采集
  ad_h = SPI_ReadWriteByte(SPI1, 0xffff);
  ad_l = SPI_ReadWriteByte(SPI1, 0xffff);
  GPIO_SetBits(GPIOA, GPIO_Pin_15); //关闭片选，屏蔽从低到高位数据，只采集，从高位到低位数据

  ad_h &= 0x000003ff; //低10位有效
  ad_l >>= 10;
  adc_value1 = ad_h << 6 | ad_l; //取16位数据  //TODO:后续看下ADS芯片手册
  return adc_value1;
}
```

修改代码有感：

* 1.0.

```C
	int ad[4];
	GPIO_SetBits(GPIOA, GPIO_Pin_15);
	ad[0] = SPI_ReadWriteByte(SPI1, 0xffff);
	ad[1]= SPI_ReadWriteByte(SPI1, 0xffff);
	ad[2]= SPI_ReadWriteByte(SPI1, 0xffff);
	ad[3] = SPI_ReadWriteByte(SPI1, 0xffff);
	return 0;
```

然后意识到CS必须是由高变低才能有反馈，增加 `GPIO_ResetBits(GPIOA, GPIO_Pin_15);`

* 2.0

```C
	int ad[4];
	GPIO_SetBits(GPIOA, GPIO_Pin_15);
	ad[0] = SPI_ReadWriteByte(SPI1, 0xffff);
	ad[1]= SPI_ReadWriteByte(SPI1, 0xffff);
	ad[2]= SPI_ReadWriteByte(SPI1, 0xffff);
	ad[3] = SPI_ReadWriteByte(SPI1, 0xffff);
	GPIO_ResetBits(GPIOA, GPIO_Pin_15);
	return 0;
```

第一次，4个数据都是FFFF,应该是正常的，看起来就是没有选中CS时的默认输出高电平
![](img/2021-02-26-14-02-95.png)
第二次复现时，4个数据都变成0000，发现是没上电，可能ADS8320没能获得5V供电，啥都无法输出，但理论上应该是先置低 `GPIO_ResetBits(GPIOA, GPIO_Pin_15);`待查PCB图

**插播一条**:
[assert_param函数](https://blog.csdn.net/mhl119/article/details/37990587?utm_medium=distribute.pc_relevant.none-task-blog-BlogCommendFromMachineLearnPai2-2.control&dist_request_id=84043717-cccd-4ff9-977b-9b23862834d6&depth_1-utm_source=distribute.pc_relevant.none-task-blog-BlogCommendFromMachineLearnPai2-2.control)

#### 2020.2.25

学习F4时钟部分相关知识
我们都是采用官方库提供的配置（外部晶振8MHz，系统配置为168MHz）

1. 在F/ 4启动文件 startup_stm32f4xx.s 中,可以看到如下:

```
Reset_Handler    PROC
                 EXPORT  Reset_Handler             [WEAK]
        IMPORT  SystemInit
        IMPORT  __main

                 LDR     R0, =SystemInit
                 BLX     R0
                 LDR     R0, =__main
                 BX      R0
                 ENDP
```

应该是在reset时就会自动进入SystemInit，再进入main

可以看到在debug中手动选择复位后，程序自动进入reset中断函数，在这里重新配置时钟啥的

憨憨如我，串口初始化注释掉了，然后直接用虚拟示波器发送程序，程序编译无问题但会运行一段时间后卡死

#### 2020.2.23

通过选择速度来选择不同的输出驱动模块，达到最佳的噪声控制和降低功耗的目的.高频的驱动电路，噪声也高，当不需要高的输出频率时，请选用低频驱动电路，这样非常有利于提高系统的EMI性能.当然如果要输出较高频率的信号，但却选用了较低频率的驱动模块，很可能会得到失真的输出信号.

关键是GPIO的引脚速度跟应用匹配（推荐10倍以上? )。

#### 2020.1.26

试图重新修改下程序,有些小细节可以修改
注意后面写程序还是用 u8/u16 这类为好，
![字节](img\字节数.jpg)

#### 2020.12.13

霍夫直线变换时间复杂度、空间复杂度都好高，将 PI 均分 180 份，再 270 个点，180\*270，再在其中投票（此处应该会有将离散的变为指定点的），不然要没一个可用就很尴尬

#### 2020.12.11

最小二乘法时，出现 0 的话就不累加
到底哪出问题了啊啊啊
实在不行就重写，气死我了
应该是最小二乘法拟合平行 y 轴直线效果就不好

#### 2020.12.10

如果某一个值测得为 0，希望用相邻两点平均值代替，不对，有可能出现连续 5 个点都是 0，那么 0 就不能参与拟合

希望得到角点坐标，偏离左侧挡板最短距离，void 吧

直接规定起点 0，终点 95，有啥问题吗，没问题

#### 2020.12.9

1.三点拟圆效果似乎不太好，后续要多点试试
(样条曲线差值？)关键词：**一堆点求曲率**

2.阈值选取先手搓

3.写个函数指定起点终点，先试试 0° 到 100°，通过返回角点数据，再进行二次拟合，就算被路过启动区的 TR 碰瓷，设置阈值也应该可以搞定

4.先用 0 到 90（单角点）找到角点，角点减 2（**存疑**），即 msg[0][corner-2]=1，再将 0 到终点进行拟合，找到距离最近点（划掉）用点到直线距离公式吧，不对，这样会出现斜率为无穷大情况，不能简单用 k，b 了
——想多了，最小二乘法垃圾

发现激光雷达可以避免安装误差

#### 2020.12.8

1.暂定用三点拟合圆，求圆心半径，用正弦定理会方便很多，暂未考虑竖直的情况
——失败 excel 发现直线部分曲率并不为很大，角点部分也不是特别小

2.打算用知乎方法，样条曲线
——待看

细节：
1.角点应该用数组记录，同时 start-角点 1 拟合，角点 2-角点 3 拟合，，，角点 n-end 拟合，判断拟合直线斜率，两夹角是否为 135

2.还要加上阈值选取，，之前采用固定阈值的方式，后来改用距离 × 角分辨率再 × 手搓的一个系数（暂选取 1.95），同时注意检查下公式有没有用错

3.注意 arctan 得到的负数应该要加 180°，比如 arctan(-1)在 excel 读作-45°
这儿还要注意下，角度 179° 其实和 1° 偏得不大，加个判断条件

先按单角点的方式拟合，如果**标准差**大于某一个阈值，分两段，再分别拟合
发现要认为起点和终点均认为是特殊点，再慢慢对下数据吧

#### 2020.12.7

先用欧阳浩方法获得连续段的 start 和 end，从 start 到 end 拟合一条直线，离直线最远处为角点
如果要计算点到直线最大值，必须要知道每个点到直线距离（点到直线距离公式？），或者先用旋转矩阵将拟合直线及点旋转到 x 轴方向，则此时 y 就是偏离直线距离
获得角点处需要直线拟合，通过角点分段后还要直线拟合，分开写吧

DividePots 获得每一段 end，start

#### 2020.11.7

#### 2020.11.1

用到多任务时可能需要用到 volatile float，//遇到这个关键字声明的变量，编译器对访问该变量的代码就不再进行优化，从而可以提供对特殊地址的稳定访问
定义在函数内部的变量为局部变量，定义在函数外部的变量为全局变量
可以看到串口发送函数一次只发送一个字节，但可以用 while 循环做到发送数组

#### 2020.10.31

可以通过壶的特征识别位置，就没必要一定识别壶口的位置。只要知道壶相对我们的位置，再加上摄像头高度就好

#### 2020.10.27

在串口发送函数里面写好对状态标志位的判断，后续直接调用就很棒

#### 2020.10.26

重新看原理图，有时候用 shift 会误判一些原理图连接，
终于明白之前检测 SPI 接口电压为 12V，原理图可以看到 SPI 中只用了 1 根线与 ADS 芯片连接
原来板子上看到的 SPI 接口是先经过 ADS 再与芯片引脚相连的
![图片](img\DT35接线.png)
感觉懂了一些，结果发现不懂的又变多了
发现简单的 ctrl+B 可以实现分割线效果，神奇

---

1. DT35 数据通过 QA1 进入 ADS 芯片，兜兜转转，QA1 进入 IN+、模拟地进入 IN-
2. ADS 芯片处理后，经过 DOUT(即 MISO 线)传回 F4 芯片（PB12、PB13 分别由 F4 芯片输出控制时钟/片选，PB14 由 ADS 芯片作为 MISO 将返回值输入芯片），其中，PB10（MOSI）根本就没接线，难怪不配置得理直气壮。
   后续要读取 MISO 数据就直接往 MOSI 随便写一个数据就好，之前还特意因为这个问过锟锟![图片](img\DT35外观图.jpg)

![奇奇怪怪](img\DT35性能.jpg)

很短时间内数值跳动 5mm 以内均为正常

注：可重复性：0.5 mm 至 5 mm
测量范围：0.05 m 至 12 m（对于自然物体），0.2 m 至 35 m（对于反光膜）
学到了，**alt+左右箭头**可以快速切换到点击连接前的位置

完了，我们的 DT35 用的是啥模式啊，看不同模式间**可重复性**图应该是有区别的，还有速度（super low/low）

![激光雷达？](img\激光雷达时序图？.jpg)

#### 2020.10.24

重新看 DT 的说明书和板子原理图，意识到之前有些理解是错误的
CAN 部分：发现底板上安装了 TCAN1051 芯片（CAN 收发器）
DT35 部分：是硬件组兄弟们帮忙加上数模转换芯片

#### 2020.10.18

<!-- 老天保佑，现在还是有一个账号可以正常使用的，算了，后续找个学长不忙的时候一劳永逸地解决了吧 -->

- 可以用 GPIO_DeInit(GPIOA);将外设寄存器初始化为其默认复位值（GPIO 引脚配置为浮动输入模式）

#### 2020.10.17

我再也不随便该系统设置了，莫名奇妙加了一个管理员账号，勉强解决了一个问题，虽然又多出一堆问题

#### 2020.10.13

![avatar](img\TXE标记位.png)
在发送后面可以加上这一句使不至于覆盖：
while(USART_GetFlagStatus(USART1, USART_FLAG_TXE) == RESET){}
![avatar](img\发送时的TC，TXE行为.jpg)

#### 2020.10.11

记得测试改写的 DT，应该，没大问题吧
还要修改滤波初始值

我再也不随便复制代码了。定位与重定位用的函数名称有些功能一致但命名有细微差别

[串口发送数据问题](https://blog.csdn.net/euxnijuoh/article/details/74923998)

此 API 函数不完善，函数体内部没有一个判断一个字符是否发送完毕的语句，而是把数据直接放入发送缓冲区，当连续发送数据时，由于发送移位寄存器的速度限制（与通信波特率有关），导致发送缓冲区的数据溢出，老的数据还未及时发送出去，新的数据又把发送缓冲区的老数据覆盖了。

#### 2020.10.10

发现 LED0=!LED0;真挺好用的，配合 while 与 delay 很容易实现闪烁效果，到时候可以用于指示运行状态

#### 2020.10.9

[GPIO 模式详解](https://blog.csdn.net/qq_38410730/article/details/79858906?utm_medium=distribute.pc_relevant_t0.none-task-blog-BlogCommendFromMachineLearnPai2-1.channel_param&depth_1-utm_source=distribute.pc_relevant_t0.none-task-blog-BlogCommendFromMachineLearnPai2-1.channel_param)

<!-- 心情之前不太好，原来靠近阳光自己也会被灼伤吗 -->

- HAL_GPIO_ReadPin ()来读取 IO 口的状态的
  HAL_GPIO_WritePin(GPIOB, GPIO_PIN_0|GPIO_PIN_1, GPIO_PIN_SET)
- 终于弄明白四种 GPIO 输入模式啦，以**按键输入**为例，如果按下去低电平有效，那么在信号设置时就应该使无信号输入时保持高电平，即应配置为上拉输入*注意浮空输入模式下，在无信号时读取端口电平是不确定的*
- 原来按键输入实验可以不用中断，，，大胆猜测后面使用 case-switch？

#### 2020.10.7

<!-- 体育课，遇见了特别可爱的慧慧姐，下午做了四个小时实验，绝望。对了，阿波罗第一个跑马灯实验成功，纪念一下 -->

keil 编译出现的错误：Error: L6218E: Undefined symbol app_uart_put (referred from main.o).

一般几种情况：

1. .c 中未定义
2. .h 中未引
3. 未包含含有该类型的.h 文件
4. 在 keil 中未添加.c 或.h 文件到工程

可以把 TFTLCD 当成 SRAM 设备用：

#### 2020.10.6

HAL 库的 systemInit 除了打开 HSI 外，没有任何时钟相关配置，即我们必须编写自己的时钟配置函数

#### 2020.10.5

[运动模型](https://zhuanlan.zhihu.com/p/89110692)

#### 2020.10.4

- 所谓速度在不同坐标系中转化其实就是同一**瞬时**速度在不同坐标轴方向的分解。给定任一瞬时速度，可以看成简单的直线运动，也可以看成纯粹的圆周运动（甚至圆周半径都是可以随意指定的，不过如果看成圆弧运动的一个瞬时，曲率半径应该要是连续的）
- 当一个任务进入延时函数后，这个任务首先要暂停（把自己的就绪状态取消），再要把 CPU 的执行权交给别的任务（把别的任务设置为就绪），这个过程就是任务的切换。
- 枚举是 C 语言中的一种基本数据类型

```C
//枚举，相当于定义周一为1，周二为2
enum DAY
{
      MON=1,
      TUE,
      WED,
      THU,
      FRI,
      SAT,
      SUN
};

```

- 任务只会运行 while(1)部分
- 全局变量
  优点：全局可见，内存地址固定，读写效率高。
  缺点：1）这破坏了函数的封装性能，降低了函数的可移植性。2）使代码可读性差，大型程序里面简直是灾难 3）生存期长，会占用较多的内存单元。
- 善用收缩功能，不然长长的 if 真让人看得头秃

[系统时钟设置](https://blog.csdn.net/maiziedu/article/details/84827945?utm_medium=distribute.pc_relevant.none-task-blog-BlogCommendFromMachineLearnPai2-2.add_param_isCf&depth_1-utm_source=distribute.pc_relevant.none-task-blog-BlogCommendFromMachineLearnPai2-2.add_param_isCf)

#### 2020.10.3

- 学到了，OSTimeDly()**进行任务切换，让比这个任务优先级更低的任务运行**。不带系统的软件延时程序会卡在当前位置一直等待延时结束，但是带系统的延时函数在延时期间系统会去运行别的任务，而不会让系统傻傻地等啥都不干。<!-- 但你究竟要等多久捏 -->
- 三个解算：姿态角解算、位置解算、速度解算应该有一个先后顺序。
  位置解算如果有旋转，必须要考虑角速度
- 需要考虑的是小车姿态角的变化，很容易发现这个就是小车上陀螺仪测得角速度积分（theta）
- OS_TICKS_PER_SEC 这个东西表示 CPU 一秒钟进行多少次时钟中断，也就决定了最小的延时间隔，比如 OS_TICKS_PER_SEC 设置成 100，则表示 1s 里面会发生 100 次时钟中断，也就是 10ms 来一次中断，则最小的延时单位就是 10ms
- #define OS_TICKS_PER_SEC 20000u 50μs 来一次中断
  在 OSTimeDly（Ticks）中调用 OS_Sched（）函数找出任务就绪列表中优先级最高的任务并将当前 CPU 切换到该任务。
- 测量任务运行时间请使用 UCOS 自带的 OSTimeGet()函数，进入函数的时候调用一次 OSTimeGet(),任务执行完以后在调用一次 OSTimeGet()，两者相减就是任务运行时间，注意两者相减的结果的单位不是 ms，而是时钟节拍数。如果要测量任务运行时间的话最好将 OS_TICKS_PER_SEC 设置为 1000。
- 注意开始任务的优先级设置为最低
  TCB:Thread Control Block

[任务调度（一）](https://zhuanlan.zhihu.com/p/36424848)

#### 2020.10.2

- 发现自己又将函数看错了，if 看成 for 难怪觉得学长写得奇奇怪怪 <!-- 不太喜欢学长这种循环的方式，为什么要在if里不循环完非要跑else里走最后一下呢 -->最后一次循环单独写的目的是便于在完成的同时修改状态标记
- 学到一招，markdown 里原来可以直接用 ctrl+/进行注释
- VG910B 陀螺仪正反向转的积分系数好像不同，**就是我们要找的**参数
- 华为 share 挺好用的，可以在平板和手机间传数据，终于不用担心这时不时就闪退的 QQHD 了，最棒的是可以实时修改，不过需要在同一个局域网下，看来要手机做两者热点，省去数据线和两用 u 盘的烦恼
- 心态爆炸，算了半天发现自己矩阵算错了，，，以后一定好好打草稿

<!-- #### 2020.10.1

上午因为触控笔出问题心神不宁，下午跑楚河汉街修理，虽然并没有查出什么，不过看看夜景，坐大循环走一圈武大也是挺棒的。这才意识到疫情半年加开学这一个多月都没好好在校园内走走 -->

#### 2020.09.30

- 可以用参数初始化函数将一揽子函数集合，然后函数前面可以加注释模板
- 终于感受到引脚电平翻转函数的作用了 GPIO_ToggleBits(GPIOB, GPIO_Pin_9)，这样很容易实现等时间的亮暗，而不用单独配置延时函数（如跑马灯中）
- 程序中角度与弧度有点傻傻分不清啊

#### 2020.09.29

- 姿态融合


| 名称         | 静止状态                 | 运动状态                   |
| ------------ | ------------------------ | -------------------------- |
| 陀螺仪       | 存在缺陷，**受**时间影响 | √                          |
| 加速度传感器 | √                        | 存在缺陷，**不受**时间影响 |

哎**后面想如何将这么多传感器（DT，激光雷达，等）融合**

解决方案：静止时增大加速度数据的权重；运动时增大陀螺仪数据的权重

- 光纤陀螺仪（圆形）——SPI——数据采集板（圆形，F4）——CAN——主控板
  测试时固定在转台上
- 预备知识：刚体不管是平动还是转动（此处 w 转化为速度），沿两点连线方向速度肯定不变，哈哈哈，我终于推出来啦，学长写的应该是没问题的
- 终于会用快捷键插入链接了，ctrl+L，第一个输入文本显示，第二个输入链接

#### 2020.09.28

- 滤波是一个自然而然地过程，频率超过截止频率后自然振幅会衰减很大，可以用传递函数，bode 图推导，幅频相频特性曲线
- 接线，中间几多痛苦，最后终于成功啦，可喜可贺。
- 看定位方面资料

#### 2020.09.27

1. 建议在.h 文件里面声明函数与 extern，这样后面 main 函数直接按习惯的调用.h 文件就好了
2. 当出现未定义情况的报错时，注意一步步检查，包括初始定义，.h 文件是否 extern，main 的 h 文件是否 include 指定文件。（幸好这种问题还是自己找到地方了）
3. 可能在 F4 的程序中程序在默默更新 kalman 系数（特指初始值？）而 F1 的程序就有点问题
4. 鸭梨山大，第一次控制组组会，有点虚

#### 2020.09.26

- 全新完整版赛题发布，一些很有意思的想法被迫舍去：不能同时 5 发，必须在一个落地/入壶后才能投另一个
- 有趣的一点，DR 在出启动区后就可以自由变形拉伸（高度）不过如果变化太多可能对控制，对资本的要求也会更高，鱼和熊掌不可兼得啊
- 意识到传递地址参数的优越性，在 VS2017 中写了自己的一个测验代码，然后出现奇奇怪怪报错的原因是把结构体定义放在了使用的后面，但它报的错却是从未见过的类型
- 用 excel 函数根据代码模拟返回值的变化趋势，结果非常 amazing 啊，不过也产生疑惑，我们得到的数据是非常完美的（反正锟锟用于 matlab 标定的就很完美），但理论上说会有波动，直至数据处理后期才会比较稳定。不过锟锟说也是有波动的，是等了一会的(原话：有啊，等一会来的)大概是浩哥的程序哪没修改好吗**后续在导轨上验证时发现误差蛮大，一气之下在 debug 中观察，excel 拟合，嗯，真香**

#### 2020.09.25

- 向锟锟大佬学习
  1.每次读取的 50 个数据，最终保存的只有 1 个（就经过那个平均滤波之后的），相当于接收 50 个数据，实际上处理了 50×50 个，笑哭

  [F4中的任务](https://blog.csdn.net/houyichaochao/article/details/80783622)

  所谓的任务，其实就是一个死循环函数，该函数实现一定的功能，一个工程可以有很多这样的任务（最多 255 个），UCOSII 对这些任务进行调度管理，让这些任务可以并发工作（注意不是同时工作！！，并发只是各任务轮流占用 CPU，而不是同时占用，任何时候还是只有 1个任务能够占用 CPU），这就是 UCOSII 最基本的功能。

  - ADS8320 芯片
  -


| 缩写 | 全称          | 备注     |
| ---- | ------------- | -------- |
| AI   | Analog Input  | 模拟输入 |
| DI   | Digital Input | 数字输入 |
| DO   | Digital Ouput | 数字输出 |
| PWR  | power         |          |
| CS   | Chip select   |          |
| SHDN | Shutdow       |          |

2.输出 16 进制数,该架构和过程允许 ADS8320 采集并以高达每秒 100,000 次转换的速度转换模拟信号，输出的还是 16 位数据，是串口传送远不能及的
3.外部时钟可以在 24 kHz（1 kHz 吞吐量）和 2.4 MHz（100 kHz 吞吐量）之间变化 4.有点奇怪，常见信号都是低频（**信号处理中经常听到低频缓变信号**）吗，一般都是滤去高频噪声。直接将输入/输出端引出一个接地电容，这样高频信号很容易就更通过电容流入地，相当于被抑制啦。

#### 2020.09.24

- 亲身算一算串口与 DMA 与 AD 转化数据的速度，有惊喜哟
  1.串口：波特率 115200，每次 8bit+1 停止位，115200/9=12800B/s
  &emsp;&emsp;即 1ms 才发送 12.8B
  2.ADS8320 芯片可以在 2.4MHz clock rate，那么 1ms 即可以发送 2.4×10^3，这数量级，悲伤

#### 2020.09.23

- 原来滤波器设计就是根据要截止的频率设计参数 R,C
- 每个在刚体中的质点具有相同的自旋角速度，此自旋角速度与刚体上或是实验室坐标系统的原点的选择无关。换句话说，这是一个刚体特质所具有的真实物理量，与坐标系统的选择无关。
  然而刚体上的参考点相对于实验室坐标原点的角速度则和坐标系统的选择有关，为了方便起见，通常选择该刚体的质心当作刚体坐标系统的原点
- 开始搞信号处理？？？
- [二阶滤波器原理及算法程序](https://blog.csdn.net/tuxinbang1989/article/details/80283857)

  [二级滤波电路 PPT](https://wenku.baidu.com/view/c2062380d0d233d4b14e699e.html)
  [三种滤波器的工作原理](http://www.elecfans.com/d/1122522.html)
- _按矩阵乘法；._ 运算符执行按元素乘法，允许您将两个大小相同的数组的对应元素相乘。

```m
z = [3 4] * [10 20] //错误使用！！！用于矩阵乘法的维度不正确。
```

- 啊啊啊，我终于明白自控为什么要用阶跃响应信号了，时域上阶跃，频域为无限，这样才能用滤波器进行频域分析啊。而且 Laplace 变换为 1，简直完美，后面利用信号可加性，增益之类的就好了
  然后伯德图就直接用 step 就好，反正没啥影响

#### 2020.09.22

- 自己终于大概明白码盘定位的作用了，应该是每次计算微小采样时间内差速移动距离得出速度、转向（两轮差动小车真可爱）

  [二轮小车航迹推演](https://blog.csdn.net/heyijia0327/article/details/47021861)
- matlab 索引数组的几种方法

```m
x=data(3,6) //选择数组第三行第六列
x=data(end-1,3) //倒数第二行第三列
x=data(:,3) //第三列所有元素
```

- 码盘安装与车子自身坐标系不一定平行（匹配得很好？）
- 小车在场上的位置-→ 世界坐标系
  车上部件位置-→ 自身坐标系
- 转动刚体任一点的速度大小，等于刚体的角速度与该点到轴线的垂直距离的乘积，它的方向沿圆周的切线而指向转动的一方（那么我们是认为小车质心就是旋转中心吗？想换个方向检测）**其实并不是这样，小车上一点相对于其他任一点旋转角速度都是一样的**

#### 2020.09.21

- 注意串口与 SPI 通信接线有区别，串口是 TxD 接 RxD（transmit 接 receive），SPI 是 MISO 接 MISO，CAN 好像也是 CAN_H 接 CAN_H（虽然好像硬件组已经帮我们搞好了线）
- clear 函数清理工作区。clc 命令清理命令行窗口。
- 可以通过输入变量的名称来查看任何变量的内容。
- 当您用空格（或逗号）分隔数值时（如前面的任务中所示），MATLAB 会将这些数值组合为一个行向量，行向量是一个包含一行多列的数组 (1×n)。当您用分号分隔数值时，MATLAB 会创建一个列向量 (n×1)。

```m
mat datafile    //使用datafile.mat保存变量
//load datafile
load myData m
save justm m    //将变量 m 保存到名为 justm.mat 的新 MAT 文件中
```

然后尝试将变量 m 保存到名为 justm.mat 的新 MAT 文件中：

#### 2020.09.20

- 采用一种特别笨的方法测试串口能否正常收发，中间希望用 for(int i=0;i<15;i++){USART_SendData(USART1,'b');}后面发现可以用 DMA 高端测试
- 终于标定完了，任务完成 2/3，奥力给！
- uC/OS-II 提供了任务管理的各种函数调用，包括创建任务，删除任务，改变任务的优先级，任务挂起和恢复等，先看看吧，希望有用

#### 2020.09.16

- MOSI 没有用到就干脆不配置了 o_o ....
- 神奇的寄存器配置，先默认为 0，后面用移位仅修改变为 1 的就好啦~傻乎乎地去问了锟锟。
- SPI_DR 寄存器是个神奇的东东，16 位的数据寄存器 SPI_DR，它对应两个缓冲区，1 个发送缓冲区，1 个接收缓冲区
- CS  决定了唯一的与主设备通信的从设备，如没有 CS 信号，则只能存在一个从设备，主设备通过产生移位时钟信号来发起通讯（感觉说的就是我们的 DThhh）
- 原来 USART_DR 也是一个双寄存器，包含了 TDR 和 RDR。
  当向 DR 寄存器写数据的时候，实际是写入 TDR，串口就会自动发送数据
  当收到数据，读 DR 寄存器的时候，实际读取的是 RDR
  TDR 和 RDR 对外是不可见的，所以我们操作的就只有 DR 寄存器

#### 2020.09.14

- 注意 matlab 两函数相乘用 .\*（与普通的区别）
- 时域与频域：音乐波形与简单的五线谱（5 线谱即表示一个固定频率的声音）

#### 2020.09.12

- 我们一般使用 SPI 时是将数据从 ADS 发送到 32 上即主输出从输入 MOSI，然后再使用 DMA 将数据从 MOSI 发到缓存器中再从缓冲器发到外设上一般为串口 1 上，我们也可以不用 DMA 使用 SPI 的数据发送函数把数据再发送到串口但是这就需要使用 MCU 了，或者就可以理解为占用了 CPU 了**此处应感谢硬件组的同学，原来我们用的 DS 芯片是自己焊到 DT 底板上的**
- 主机的 NSS 作为 GPIO 口使用，在进行 SPI 初始化时设置为 SPI_NSS_Soft。从机的 NSS 由主机 NSS 管脚的输出状态决定，在进行 SPI 初始化时设置为 SPI_NSS_Hard
- stm32 作为主机进行配置，DT 作为从机应该也行，到时就是从机发送数据主机进行接受（反正是全双工）SCK 和 MOSI 管脚设置为复用推挽输出；NSS 作为 GPIO 口使用，设置为推挽输出
- 寄存器小技巧
  GPIOB->ODR ^= GPIO_Pin_8 将 PB8 取反
  GPIO_Pin_n 数值其实就是 16 位寄存器哪位置 1 就哪位，^符号异或操作，原本为 0 就不变，原本为 1 便取反
  端口输出数据寄存器(GPIOx_ODR)，哪位 1 就哪位

#### 2020.09.11

加油将几种数据传输方式整理下!!!matlab 技巧：编辑-坐标区属性-网格-x/y_grid 打 √ 即可

- 预备知识

&emsp;SPI 外设的写操作和读操作是同步完成的。如果只进行写操作，主机只需忽略接收到的字节；反之，若主机要读取从机的一个字节，就必须发送一个空字节来引发从机的传输
后面重新看 DT 板子原理图，发现 MOSI 对应线（PA？）根本没有连接，难怪也不考虑配置


| 缩写 | 全称                      | 备注                           |
| ---- | ------------------------- | ------------------------------ |
| MISO | Master Input Slave Output | 主设备数据输入，从设备数据输出 |
| MOSI | Master Output Slave Input | 主设备数据输出，从设备数据输入 |
| SCLK | Serial Clock              | 时钟信号，由主设备产生         |
| CS   | Chip Select               | 从设备使能信号，由主设备控制   |

#### 2020.09.08

- 意识到黑箱实验优点？好像确实没必要将一切参数都弄得明明白白
- 被告知去年没用卡尔曼滤波，困惑于前几天的努力（不过很开心把 0808 那个 matlab 程序弄明白了，还有锟哥那个神奇的大杂烩）

#### 2020.09.07

终于开始啃卡尔曼滤波的 stm32 程序了，明白了初始值的应用，晚上继续看

1. matlab 数组
   1.1 创建二维数组 A
   &emsp;&emsp;**创建任一数组时，建议先用 zeros()函数初始化**
   &emsp;&emsp;A=[1:3;2,3,4] **分号表示换行**
   &emsp;&emsp;A=zeros(3,3) **全 0 项**
   &emsp;&emsp;A=ones(3,3) **全 1 项，非单位向量**

   1.2 冒号作用
   1:2:10 **从1开始，每次加2，加到10**
   linspace(4,15,3) **从 4 到 15 等差，一共 3 个数**
   1.3 创建三维数组
   &emsp;&emsp;需通过指定索引把二维数组拓展成多维的

   A1=[1,2,3;4,5,6;7:9]
   A2=[10:12;13:15;16:18]
   A3=-[1,2,3;4,5,6;7:9]
   A4=cat(3,A1,A2,A3)
   即有A4(1,2,3)=-2

   1.4 数组元素的索引与寻址

   2. switch case otherwise end**无需用 break 结束**；
   3. function [输出参数 1，输出参数 2······]=函数名（输入参数 1，输出参数 2······）

   #### 2020.09.06

   D:\Exercise\MATLAB\follow_CAN 中 excel
   第一次开全体会议+个人介绍，瑟瑟发抖

   #### 2020.09.05

   明天要工训开卷考试呜呜呜
   (工训结束啦，原来开卷考试可以查手机 hhh)

   #### STM32F40x 部分


   1. 系统默认时钟及使能
      如要使能 GPIOA,只需要在 stm32f4xx_rcc.h 头文件里面搜索 GPIOA
      注意：F1 与 F4 系统时钟不同，5 个外设时钟使能函数（AHB1/AHB2/AHB3/APB1/APB2）


   | F429                                                 | 备注           | 大小   |
   | ---------------------------------------------------- | -------------- | ------ |
   | SYSCLK                                               | 系统时钟       | 180MHz |
   | AHB 总线时钟                                         | HCLK=SYSCLK/1  | 180MHz |
   | APB1 总线时钟                                        | PCLK1=SYSCLK/4 | 45MHz  |
   | APB2 总线时钟                                        | PCLK2=SYSCLK/2 | 90MHz  |
   | PLL 主时钟                                           |                | 180MHz |
   | 这儿有遗留问题，有些程序学长们忘记将 72M 改成 84M 了 |                |        |
   | F405                                                 | 备注           | 大小   |
   | -------------                                        | -------------- | ------ |
   | SYSCLK                                               | 系统时钟       | 168MHz |
   | AHB 总线时钟                                         | HCLK=SYSCLK    | 168MHz |
   | APB1 总线时钟                                        | PCLK1=SYSCLK/4 | 42MHz  |
   | APB2 总线时钟                                        | PCLK2=SYSCLK/2 | 84MHz  |
   | PLL 主时钟                                           |                | 168MHz |


   | F103          | 备注           | 大小  |
   | ------------- | -------------- | ----- |
   | SYSCLK        | 系统时钟       | 72MHz |
   | AHB 总线时钟  | HCLK=SYSCLK    | 72MHz |
   | APB1 总线时钟 | PCLK1=SYSCLK/2 | 36MHz |
   | APB2 总线时钟 | PCLK2=SYSCLK   | 72MHz |
   | PLL 主时钟    |                | 72MHz |

   2.GPIO 设置（寄存器版）
   BSRR 只写寄存器：
   对寄存器高 16bit 写 1 对应管脚为低电平，对寄存器低 16bit 写 1 对应管脚为高电平。写 0 ,无动作

   #### 2020.09.04

   控制部分：

   - PID 控制

   eg.1 推箱子模型

   ①P：比例控制
   最好距离远时推力大，距离近时推力小（推力与距离成正比）
   **问题：考虑摩擦力，距离近到一定程度时无法推动（存在静差）**

   ②I：积分控制（**累加消除静差**）
   假设我们每秒观测一次当前位置和目的地的距离，我们将每次的距离值累加起来。上一秒距离 5 米，这一秒还是距离 5 米，则累加值就会是 10 米，下一秒距离如果还是 5 米，则累加值就是 15 米。将累加值乘以一个系数添加到推力中，则随着时间的延长，终有一刻会再次推动箱子。最终实现哪怕有一丁点的静差，都会被累加值消除掉，使得控制对象精准等于设定量

   ③D：微分控制（距离变化率（速度）就是距离的微分）
   我们将箱子的距离的变化率也乘以一个系数作为阻力，将这个阻力叠加到推力上，如果上一时刻的距离和这一时刻的距离相差太大，即箱子推动速度太快了，推力就会因为阻力大而变小一点；如果上一时刻的距离和这一时刻的距离相差不太大，推力就会因为阻力小而基本不受影响。这样就可以**避免快到终点时因为用力过猛而推过头**。

   **位置式 PID 和增量式 PID**:

   - 通常位置式 PID 用于执行机构不会将 PID 输出值累积起来的系统
     PID 输出一个值给 PWM，PWM 控制电机产生一个速度，然后 PID 再输出一个值给 PWM，PWM 控制电机产生另一个速度，后者的速度不会在前者的速度基础上累加，二者相互独立。
   - 增量式 PID 用于执行机构会将 PID 输出值累积起来的系统
     当 PID 输出一个值给步进电机，步进电机会转过一定的角度，然后 PID 再输出一个值给步进电机，步进电机会在上一刻转角基础上再转过对应的角度，即执行机构会将历史值累积起来

   #### 2020.09.03

   ##### matlab 部分

   **Excel** NB

   1. strcat：连接字符串的函数 S
   2. 读入与写入函数（fopen&fprintf）
      fprintf：将数据按指定格式写入到文本文件中
      fopen：将数据按指定格式读入到 matlab 中的函数。

   ```M
   fprintf(fid,format,A)
   ```
   fid:文件句柄，指定要写入数据的文件若缺省，则输出到屏幕

   format ：控制所写数据格式的格式符


   | 类型 | 说明       | 备注                           |
   | ---- | ---------- | ------------------------------ |
   | %d   | 整数       |                                |
   | %e   | 实数       | 科学计数法形式                 |
   | %f   | 实数       | 小数形式                       |
   | %g   | 实数       | 由系统自动选取上述两种格式之一 |
   | %s   | 输出字符串 |                                |

   A:存放数据的矩阵

   3.cftool 进行曲线拟合


