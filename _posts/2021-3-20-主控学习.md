# 主控学习

## 常用链接

[利用LaTeX插入公式](https://blog.csdn.net/happyday_d/article/details/83715440)

## 预备知识



### 速度解算

1. 设计的是小车**质心**相对于全场坐标系下的速度，首先转化为小车相对于自身坐标系下的速度（大概因为转舵角啥的都是相对于小车坐标系）

   感觉和力的分解有点像哦，表征的是同一个矢量，但分解到不同方向就有不同的表示方式

2. <img src="https://i.loli.net/2021/03/25/Ui6Xv1g2wTINpqd.jpg" style="zoom:50%;"/>

3. 则有

   $$
   \begin{equation}
   \left\{
   \begin{array}{l}
   		R_x=V_x*cos(\theta)+V_y*sin(\theta)\\
   		R_y=V_y*cos(\theta)-V_x*sin(\theta)\\
   \end{array}
   \right.
   \end{equation}
   $$



### 路径规划

#### 预备知识

1. 

#### 跑直线

##### NOTICE:

1. 我们一般跑直线都是从0开始的，但驱动器似乎蛮强的，直接按速度V1进行赋值也能跑起来（**还快一点**）

2. 从0开始的加速，那么减速距离也是直接与加速距离相关。胡凯鹏是认为自动模式下减速a与加速相等，最后算出减速距离x_dc

   ```c
   p[0]=act_x-Line_P_Para.x_0;
   p[1]=act_y-Line_P_Para.y_0;
   p[2]=sqrt(p[0]*p[0]+p[1]*p[1]);
   p[3]=act_v*act_v/2.f/p[2];
   Line_P_Para.x_dc=abs((Line_P_Para.v1-Line_P_Para.v2)*(Line_P_Para.v1+Line_P_Para.v2))/2.f/p[3];
   ```

3. 之前以为我们是跑直线然后相切个圆再直线。后来问过说是可以直接跑两个折线——只要**PID**够好也能比较顺滑。

##### 路径规划

1. 考虑路径中肯定会存在垂直x轴直线这样特殊的线，目前只能用$Ax+By+C=0$来表示。已知两个点的坐标，此时有三个未知量，显然有一个是多余的。这个多余的量可以简化为1或-1以简化计算量

2. 当两点成一普通直线时，将B简化为-1，则$y=Ax+C$就是普通斜截式，有
   $$
   \begin{equation}
   \left\{
   \begin{array}{l}
   		A=\frac{\Delta y}{\Delta x}\\
   		B=-1\\
   \end{array}
   \right.
   \end{equation}
   $$

3. 当两点连线几乎垂直于$x$轴时，此时斜截式$k$几乎为无穷大，这也是不能用普通斜截式的原因

   $$
   \begin{equation}
   \left\{
   \begin{array}{l}
   		A=1\\
   		B=-\frac{\Delta x}{\Delta y}\\
   \end{array}
   \right.
   \end{equation}
   $$

4. 当$Ax+By+C=0$已经知道$A,B$后，$C$很容易就能求出

   $$
   C=-Ax_0-By_0
   $$

##### 速度规划

1. 有一个末端速度，末端速度为0时进入锁位置模式，也可能不为0紧跟着跑下一条轨迹。速度方向不一定要相切，PID似乎能跟上。
2. 速度理论上是一个先加速再减速的过程（梯形），必然存在加速时加速度，减速时加速度，匀速时速度。问过胡凯鹏，我们对于加速是给定加速度，减速是给定减速距离（据说可以看之前学长代码）加速距离和减速距离就靠手搓，，，**只要轮子不打滑**加速度可以给很大。虽然跑直线一般从静止状态下出发，但刚开始速度很小的话可能车没反应，直接干过去据说还会快些。
3. <img src="https://i.loli.net/2021/03/23/Ow5ehNBJUmsKtD1.jpg" alt="img" style="zoom:50%;"/>
4. 再在计算随时间变化的速度时应该用$\Delta t$而不是$t$，可能不同时间段加速度不一样。

##### PID计算

1. <img src="https://i.loli.net/2021/03/20/TpCNzAQsYjJ6Zhq.jpg" alt="img " style="zoom:80%;"/>

   已知直线方程$Ax+By+c=0$则当前点到直线距离$\Delta=\frac {\vert Am+Bn+C\vert}{\sqrt(A^2+B^2)}$，则点M到点P横坐标偏差$\Delta x=\Delta*sin\theta$，$\Delta y=\Delta*cos\theta$

   其中，$sin\theta=\frac{-A}{\sqrt(A^2+B^2)}$，$cos\theta=\frac{B}{\sqrt(A^2+B^2)}$

   则有$\Delta x=\frac{-A*\vert Am+Bn+C\vert}{A^2+B^2}$，$\Delta y=\frac{B*\vert Am+Bn+C\vert}{A^2+B^2}$

   显然又出现一个问题，$A^2+B^2$此处作为分母，如果很小的话会使值变得格外大。**但是**，之前有规定$\vert A\vert$或$\vert B\vert$为1，应该可以不用判断
   
2. 计算点到直线垂点坐标

   已知直线方程$Ax+By+C=0$，求点$(m,n)$到直线的垂足坐标

   则：$(\frac{B^2m-ABn-AC}{A^2+B^2},\frac{A^2n-ABm-BC}{A^2+B^2})$

   试图简化代码，则记$p[0]=An-Bm$，则坐标为$(\frac{-p[0]*B-AC}{A^2+B^2},\frac{p[0]*A-BC}{A^2+B^2})$

#### 贝塞尔曲线

#### 旋转

### U-cos

#### 信号量集

|            |                              |                                                              |
| ---------- | ---------------------------- | ------------------------------------------------------------ |
| 创建信号量 | `RUN=OSSemCreate(0);`        | 创建信号,函数的返回值为已创建的信号量的指针,0表示置位计数器初值为0 |
| 请求信号量 | `OSSemPend(RUN,0,ErrorRUN);` | 参数timeout被设置为0，任务的等待时间为无限长。               |
|            |                              |                                                              |

在程序中定义全局变量来作为信号量，并根据改信号量的访问状态来访问共享资源，以解决冲突问题。即任务A不会在任务B访问共享资源s期间来访问s。这样就避免了多个任务使用同一个共享资源时所出现的冲突现象。

任务需要访问一个共享资源时，先要请求管理该资源的信号量，这样就可以根据信号量当前是否有效（即信号量的计数器`OSEventCnt`的值是否大于0）来决定该任务是否可以继续运行。
如果信号量有效（即信号量的计数器OSEventCnt的值大于0），则把信号量计数器减1，然后继续运行任务。
如果信号量无效（即信号量的计数器OSEventCnt的值等于0），则会在等待任务表中把该任务对应的位置1而让任务处于等待状态，并把等待时限timeout保存在任务控制块TCB的成员OSTCBDlIy

任务获得信号量，并在访问共享资源结束以后，必须释放信号量。释放信号量也叫做发送信号量。发送信号量须调用函数OSSemPost（）。

函数OSSemPost（）在对信号量的计数器操作之前，首先要检查是否还有等待该信号量的任务∶如果没有，则将信号量计数器OSEventCnt加1；如果有，则调用调度器OS_Sched（）去运行等待任务中优先级别最高的任务。

eg：

编写一个应用程序，其中有一个函数Fun（）和两个任务（MyTask())与任务YouTask())）。应用程序中的两个任务都可以调用函数Fun())，但不能同时调用。

```c
OSSemPend(RUN,0,ErrorRUN);//请求信号量
OSSemPost(RUN);//发送信号量
```

PID肯定是一直在运行的，但跑直线/跑点/跑圆等肯定不能调用同一个函数？走一段路线前申请信号量，走完后释放信号量

使用举例：

```c
/*错误占位*/
#defineCAN1_QUEUE_FULL0x0001
#defineCAN2_QUEUE_FULL0x0002
#defineCAN1_NODE_LOSE0x0004
#defineCAN2_NODE_LOSE0x0008
#defineOFF_TRACK0x0010
#defineMOTOR_ERR0x0020
#defineUART4_LOSE0x0040
#defineUART5_LOSE0x0080
```

其实就是1<<1，1<<2，···，后面就可以用或操作直接将对应位置1

```c
FlagPara.Error_Flag\vert=CAN2_QUEUE_FULL;
```

### 串口屏

#### 串口控制

对串口屏按键操作其实是发送具有指定包头包尾的数据，可以在$usart.c$文件接收中断进行判断。

| BUF[5] | BUF[4] | BUF[3] | BUF[2] |  BUF[1]  |  BUF[0]  |
| :----: | :----: | :----: | :----: | :------: | :------: |
|        |        |        |        | 键盘按键 | 矩阵键盘 |
| 控件ID |        | 页面ID |        |          | 屏幕触控 |

##### 注意：

使用switch-case语句时注意别漏掉break

#### 板子发送

```c
/*包头*/
USART.TxBuffer_UASRT2[i++]=0xee;
USART.TxBuffer_UASRT2[i++]=0xb1;
USART.TxBuffer_UASRT2[i++]=0x12;
USART.TxBuffer_UASRT2[i++]=0;
USART.TxBuffer_UASRT2[i++]=0;//页面ID
USART.TxBuffer_UASRT2[i++]=0;
USART.TxBuffer_UASRT2[i++]=1;//按键ID
USART.TxBuffer_UASRT2[i++]=0x00;

/*data存储待发送的数据*/
sprintf(data,"%s","具体内容");
USART.TxBuffer_UASRT2[i++]=strlen(data);
strcpy((char*)(&USART.TxBuffer_UASRT2[i]),str_Err);
	i+=strlen(str_Err);

/*包尾*/
USART.TxBuffer_UASRT2[i++]=0xff;
USART.TxBuffer_UASRT2[i++]=0xfc;
USART.TxBuffer_UASRT2[i++]=0xff;
USART.TxBuffer_UASRT2[i++]=0xff;
```

##### 注意

1. 使用动态变化的效果判断板子是不是连接上了。如果蓝牙正常连接，主屏幕上就会动态价值。同时在多任务那也可以根据特定灯的闪烁判断。——下完程序后板子与程序时间有点延时，可能是程序中加延时的原因。

 ```c
if(order[0]<4)order[0]++;
elseorder[0]=1;

switch(order[0])
{
case1:
sprintf(str_Well,"%s","WELL");
break;

case2:
sprintf(str_Well,"%s","WELL-");
break;

case3:
sprintf(str_Well,"%s","WELL--");
break;

case4:
sprintf(str_Well,"%s","WELL---");
break;

default:
break;
}
 ```

### ELMO

1. 驱动器ID：用上位机发送pp[13]，会返回一个ID
2. 常用命令：

```c
#defineElmo_Enable(ID,waitflag,state)Commend(ID,'M','O',waitflag,state)/*使能|失能Elmo电机*/
#defineElmo_MODE_Select(ID,waitflag,mode)Commend(ID,'U','M',waitflag,mode)/*Elmo运动模式*/
#defineElmo_Reset_position(ID,waitflag,position)Commend(ID,'P','X',waitflag,position)/*重置Elmo绝对位置*/
#defineElmo_JV_speed(ID,waitflag,speed)Commend(ID,'J','V',waitflag,speed);/*设置Elmo速度模式运动速度*/
#defineElmo_PTP_speed(ID,waitflag,speed)Commend(ID,'S','P',waitflag,speed)/*设置Elmo位置模式运动速度*/
#defineElmo_PTP_position(ID,waitflag,position)Commend(ID,'P','A',waitflag,position)/*设置Elmo位置模式运动位置*/
#defineElmo_Begin(ID,waitflag)Query(ID,'B','G',waitflag)/*Elmo开始运动*/
#defineElmo_Stop(ID,waitflag)Query(ID,'S','T',waitflag)/*Elmo停止运动*/
```

2. 速度控制与转矩控制：

   [伺服电机](http://www.nimotion.com/knowledge/20/65.html)
**速度模式**是指变频器以控制电机的转速为目的，此时电机的力矩必须为保持该速度而调整。所以控制系统中外环为速度环，内环为电流环。速度环的输出为电流环的给定（力矩给定），该电流环也称为转矩环。
   **转矩模式**是指变频器是以控制电机的输出力矩为目的，速度大小和外部负载大小有关。此时变频器一般无速度环，只有电流环，外部给定直接给电流环作为力矩设定。
   
   **底盘上一般用速度模式，机构上用位置模式较多**

   现在胡凯鹏车上用elmo——速度模式，取箭放箭用elmo——位置模式。elmo毕竟是商用驱动器，比DJ3508和DJ2006自带的电调强太多。精确位置的话用elmo比较合适

3. 速度模式：失能(MO=0)—选定速度模式(UM=2)——选定速度(JV=1000)——开始(BG)

4. ![image-20210407205433250](https://i.loli.net/2021/04/07/aU8YNgxLFEdXeVs.png)

软件命令速度模式（UM=2）下，BG激活上一次的**持续运动速度**（JV）以及新的平滑因数（SF）、加速度（AC）和减速度（DC）。

注意速度模式下赋速度用的是JV=···，再用**使用BG命令**，电机就能以刚赋的JV进行运动

即修改速度后一定要用**BG**，看到队列中中有如果允许刹车，那么碰到BG命令就不发送，以确保刹车能刹住。

|      | 备注                                   |
| :--: | :------------------------------------: |
| MO=0 | 失能，电机自由转动                     |
| MO=1 | 使能，电机受到控制                     |
| BG   | 开始，可以用来激活上一次的持续运动速度 |
|ST|停止，电机速度降为0，仍是使能状态|



|       |            |                        |
| ----- | ---------- | ---------------------- |
| RPDO2 | 主控发送   | 设定驱动器值并从中获取 |
| TPDO2 | 驱动器返回 |                        |

主控板会发送两种类型报文——查询~命令

命令语句一般8个字节，前四字节命令('M''O')，后四字节数据(0x01)

查询语句一般4个字节，直接为前4字节的命令

注意前四个字节的命令是大写的ASCII，用'M''O'等直接设置也可以，后四个字节是数据，先低位后高位`EncodeFloatData(&data, &can2_sendqueue.head[can2_sendqueue.Rear].Data[4]);`

底盘一般不用elmo反馈速度（速度一般是用陀螺仪码盘），要查询只会是电流

注意一下，发送给elmo的命令中速度单位为cnt/s，我们一般用的都是轴后速度（rpm），每分钟多少转。

```c
Jv = ELMOmotor[ID].intrinsic.PULSE / 60 * speed * ELMOmotor[ID].intrinsic.RATIO;
```

$1cnt/s=60cnt/min=\frac{60}{pulse}r/min$

一般T-motor系列电机都没带减速比，即跑车轮子时都是猛跑呀

包括位置模式，elmo计算方法是cnt/s

```c
Pa = ELMOmotor[ID].intrinsic.PULSE / 360 * angle * ELMOmotor[ID].intrinsic.RATIO;
```

eg: BG命令    

DLC=4时，

| Byte      | 0    | 1    | 3    | 3    |
| --------- | ---- | ---- | ---- | ---- |
| Hex value | 42   | 47   | 0    | 0    |

那么在

### CAN队列

1. ACK位为1表示该指令帧需要响应。功能码为原功能码+0x40

2. waitflag

   有的动作需要反馈，像DT的重定位，要是没收到反馈信号车就直接尬住。跑elmo时就还行

3. 快乐啊，主控发大写字母，从机answer_master时+0x20就是小写字母



## 程序学习

1. 根据规划路径，加上自己设定的速度变化曲线，可以有一个理想的速度`out_v`，根据路线（eg直线斜率）获得理想速度的分量`Coo_X.expVal`，`Coo_Y.expVal`。**但是**，小车在运动的时候~~大概率~~肯定会偏移，参考x，y方向的偏移量可以利用PID获得x，y方向的速度增量（），再把这个量按x/y方向叠加到理想分量值上。
2. 陀螺仪返回角度制（精度更高，角度制的180°在弧度制中仅仅为3.14），但在利用$v=w*r$时，必须要用弧度制
3. 将车中心速度引出到三个轮子上，但其实赋值的只能是一个方向（车轮毂），另外方向速度应该是随动（全向轮就是牛）
4. 一个悲伤的故事；主控直接对elmo进行控制，我们算的是编码器。轮子转一圈编码器是$4096×4$，再考虑将**cm**/s转化为电机转速rpm/min——码盘反馈的速度/位置都是以cm为单位。控制elmo是直接输入[-20,000,000…20,000,000] 计数/秒

5. $1cm/s=60cm/min=\frac{60}{2*\pi*R}rpm$(即转/分钟)

### **注意**，

#### CAN

1. CAN2要在CAN1初始化后初始化
2. CAN1主要与传感器通信，CAN2主要与底盘电机及翻越电机通信
3. CAN总线占有率在30%以下会好些？之前T车底盘接近70%时会丢报文，机构60%就还好（第一次知道CAN分析仪还可以用来看这个）

#### 数据类型

1. CAN发送时，从`CanTxMsg`结构体中可以看到，发送数据类型`uint8_tData[8]`，但实际上返回数据位置角度等都有正负，如定位`tx_message.Data[2]=(uint8_t)((int16_t)(position->X*10));`

   ```c
   typedefint16_ts16;
   ```

2. 注意u16和s16的区别，一个是unsigned无符号类型，一个是signed有符号类型

#### PID

1. PID基本原理

   ![PID](https://i.loli.net/2021/03/16/Ru57LrdGUbxl3Vm.png)

   $$
   u(t)=Kp[e(t)+\frac{1}{Ti}\int_{0}^{t}e(t)dt+Td\frac{de(t)}{dt}]
   $$

2. 位置式~增量式

   |  类型  | 示意图 |   备注   |
   | :----: | :----: | :------: |
   | 位置式 |        | 小车控制 |
   | 增量式 |        | 电机控制 |

3. 抗积分饱和PID控制器

   <imgsrc="https://i.loli.net/2021/03/16/VLcS7oUAWiNsvGT.png"alt="image-20210316183711307"style="zoom:50%;"/>

   所谓积分饱和就是指系统存在一个方向的偏差，PID控制器的输出由于积分作用的不断累加而扩大，从而导致控制器输出不断增大超出正常范围进入饱和区。当系统出现反响的偏差时，需要首先从饱和区退出，而不能对反向的偏差进行快速的响应。

   为了解决积分饱和的问题，人们引入了抗积分饱和的PID算法。所谓抗积分饱和算法，其思路是在计算U(k)的时候，先判断上一时刻的控制量U(k-1)是否已经超出了限制范围。若U(k-1)>Umax，则只累加负偏差；若U(k-1)<Umin，则只累加正偏差。从而**避免控制量长时间停留在饱和区**

4. 串级PID

   ![image-20210316190047663](https://i.loli.net/2021/03/16/3UloFYc6gtqNPSB.png)

   (1)由于内环回路的存在，改变了原来的对象特性，使内环回路对象的等效时间常数变小，所以使系统的**过渡时间缩短**了，**控制作用更加及时**；
   (2)改善了对象特征，起到**超前控制**的作用，有效抑制内环回路干扰，这种超前控制作用最适合干扰落在内环回路内的情况，若干扰落在外环回路时，超前作用就不明显了；
   (3)提高了系统的工作频率，使振荡周期减小，调节时间缩短，系统的**快速性增强**；
   (4)当模型失配时，内环控制器可以很好的抑制干扰，而外环控制器则以良好的动态性能和鲁棒性能为设计目标；
   (5)由于串级控制系统的内环是一个随动控制系统，它的设定值随着外环控制器的输出而变化。外环控制器可以按照操作条件和负荷的变化情况，不断调整内环控制器的设定值，从而保证在操作条件和负荷发生变化的情况下，控制系统仍有较好的控制效果。

### 疑问

定时器初始化

### 其他

1. 多用集成好的程序？

```c
voidAsk_Dt(u8xy_flag,u8waitflag,intnum)

can1_sendqueue.head[can1_sendqueue.Rear].Id=0X01314150\vertxy_flag;//利用xy_flag修改发送ID就很棒
```

2.  总能遇到一些奇妙的方式

```c
Calcu[FlagPara.Mode_Run]();

void(*Calcu[7])(void)={Calculate_STANDBY,Calculate_PTP,Calculate_Line_Point,Calculate_Bezier,Calculate_Circle,Calculate_Wheel,Calculate_Turn};
```

再分别定义这些函数就很棒

```c
voidCalculate_Line_Point(void)
```

3. Act_C是直接读取陀螺仪码盘数据获得的实际值，注意此时读到的角度为陀螺仪读取数据的累加量（比如读个原地三连转来着）
4. 在写平时程序时注意分模块串口屏控制，上场比赛时再集成好就行。
5. 可以在定义标志位时先`Cooridnate_X.Proc_Flag=PID_|FILTER_;`，后面`if(Data->Proc_Flag&FILTER_)`就可以很方便判断了
6. 

## 迷惑现象

1. 用code打开文件夹，内部全局搜索可以搜到未打开的文件；但用KEILPROJICT打开工程，全局搜索就只能搜打开的文件。
2. 曾经Keil的5.31版本运行激光雷达出现过问题；还有阿波罗板子不要用显示屏输出太多东西，曾经有过一直进不去读取，后来一注释掉就好啦

